# Sessão 24–25/jul/2026 — resumo

Tudo abaixo está **em produção** (`main`, deploy Vercel). Migrations aplicadas no
banco via MCP `apply_migration` **e** commitadas no repo.

## Shipado

### Chat interno v1 (merge `52c5621`)
Canais **avisos / equipe / evento** com Web Push. UI: 3 abas fixas
**Geral / Eventos / Equipes** + sub-abas roláveis (culto/equipe), badge agregado,
lembra a última aba/sub-canal (localStorage), balão com nome agrupado + hora
compacta inline, painel de tamanho fixo. Eventos = **escalado OU equipe que
lidero** (admin puro não). Migrations `0035_chat` e `0037_chat_evento_lideres`.
Arquivos: `src/lib/chat.ts`, `src/components/chat/*`, `actions.ts`, `push.ts`.

### Fix de onboarding (merge `9602ef7`, migration `0036`)
`reconciliar_onboarding()` roda no `getSession` de quem ainda não está ativo:
destrava (a) convidado que logou sem o convite casar e (b) conta órfã (auth sem
profile). O trigger `handle_new_user` só provisiona no 1º signup — daí o problema.
Incidentes resolvidos: **grazi** (aprovada em Equipes → "Já logou · aguardando")
e **Luana** (conta órfã; re-login casa o convite pendente).

### Admin QoL (merge `204cf28`)
- Equipes: **recolher/expandir** cada equipe + "Recolher todas" (localStorage).
- Home admin: **calendário navegável** por mês (`/inicio?m=YYYY-MM`, resumo
  acompanha, hero fixo); clicar no dia → "Adicionar evento nesse dia"
  (`/escalas/novo?data=`); "Criar evento" virou "+" discreto; "Convidar" saiu
  da Home (já está em Equipes).

### Balanço do mês + Cronograma (merge `673f6eb`)
- **Balanço do mês** (`/balanco`, por equipe): abas **Por pessoa** (barra de
  quantas vezes + quem não foi escalado + detalhe dia·posição·com quem) e
  **Por data** (cada culto expande a composição posição·pessoa).
  `listTeamMonthAssignments` em `data.ts`; link em Escalas.
- **Cronograma**: (1) fix do **toque** (arrastar/redimensionar não abre mais o
  modal — `suppressClickRef`); (2) **faixa de eventos** deslizável que virou o
  **cabeçalho** da caixa; (3) **cabeçalho compacto** — Arquivos/Escala/Editar
  viram 3 ícones (pasta·escala·engrenagem) na linha "Ordem do culto", agora a
  linha de comando (Iniciar + relógio maiores); (4) **auto-encerrar** ao ticar
  o último bloco.

## Estado do banco
Migrations aplicadas: até **`0037`** (0034 push subs, 0035 chat, 0036 onboarding,
0037 chat-evento-líderes). Próxima = `0038`.

## Próximo
**#3b — Grade do mês** (web + export PDF/impressão): matriz completa do mês pra
tela grande. Depois **#3c drag-and-drop** (parqueado). Visão "todas as equipes
juntas" no Balanço também fica pro futuro.
